-- Rayfield laden
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔪 CoreX | Mörder Mystery",
   LoadingTitle = "CoreX wird geladen...",
   LoadingSubtitle = "Mörder Mystery 2",
   Theme = "Default",
   DisableRayfieldPrompts = false,
   ConfigurationSaving = {
      Enabled = false,
   },
   Discord = {
      Enabled = false,
   },
   KeySystem = false,
})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- ════════════════════════════════
-- SPEED TAB
-- ════════════════════════════════
local SpeedTab = Window:CreateTab("⚡ Speed", 4483362458)

SpeedTab:CreateSlider({
   Name = "Walk Speed",
   Range = {16, 150},
   Increment = 1,
   Suffix = "WS",
   CurrentValue = 16,
   Flag = "WalkSpeed",
   Callback = function(val)
      local char = LocalPlayer.Character
      if char then
         local h = char:FindFirstChildOfClass("Humanoid")
         if h then h.WalkSpeed = val end
      end
   end,
})

SpeedTab:CreateSlider({
   Name = "Jump Power",
   Range = {50, 400},
   Increment = 5,
   Suffix = "JP",
   CurrentValue = 50,
   Flag = "JumpPower",
   Callback = function(val)
      local char = LocalPlayer.Character
      if char then
         local h = char:FindFirstChildOfClass("Humanoid")
         if h then h.JumpPower = val end
      end
   end,
})

SpeedTab:CreateSlider({
   Name = "Gravity",
   Range = {10, 196},
   Increment = 1,
   Suffix = "G",
   CurrentValue = 196,
   Flag = "Gravity",
   Callback = function(val)
      workspace.Gravity = val
   end,
})

SpeedTab:CreateToggle({
   Name = "Infinite Jump",
   CurrentValue = false,
   Flag = "InfJump",
   Callback = function(state)
      UserInputService.JumpRequest:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if char then
            local h = char:FindFirstChildOfClass("Humanoid")
            if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end
         end
      end)
   end,
})

SpeedTab:CreateToggle({
   Name = "No Clip",
   CurrentValue = false,
   Flag = "NoClip",
   Callback = function(state)
      RunService.Stepped:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if char then
            for _, p in pairs(char:GetDescendants()) do
               if p:IsA("BasePart") then p.CanCollide = false end
            end
         end
      end)
   end,
})

SpeedTab:CreateToggle({
   Name = "Fly Modus (WASD + Space/Ctrl)",
   CurrentValue = false,
   Flag = "Fly",
   Callback = function(state)
      local char = LocalPlayer.Character
      if not char then return end
      local hrp = char:FindFirstChild("HumanoidRootPart")
      if not hrp then return end
      if state then
         local bg = Instance.new("BodyGyro")
         bg.MaxTorque = Vector3.new(9e9,9e9,9e9); bg.P = 9e4; bg.Parent = hrp
         local bv = Instance.new("BodyVelocity")
         bv.Velocity = Vector3.zero; bv.MaxForce = Vector3.new(9e9,9e9,9e9); bv.Parent = hrp
         RunService.Heartbeat:Connect(function()
            if not state then bg:Destroy(); bv:Destroy(); return end
            local cam = workspace.CurrentCamera; local spd = 50; local vel = Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then vel += cam.CFrame.LookVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then vel -= cam.CFrame.LookVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then vel -= cam.CFrame.RightVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then vel += cam.CFrame.RightVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then vel += Vector3.new(0,spd,0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then vel -= Vector3.new(0,spd,0) end
            bv.Velocity = vel; bg.CFrame = cam.CFrame
         end)
      end
   end,
})

SpeedTab:CreateToggle({
   Name = "Auto Respawn",
   CurrentValue = false,
   Flag = "AutoRespawn",
   Callback = function(state)
      RunService.Heartbeat:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health <= 0 then wait(1); LocalPlayer:LoadCharacter() end
         end
      end)
   end,
})

-- ════════════════════════════════
-- ESP TAB
-- ════════════════════════════════
local ESPTab = Window:CreateTab("👁 ESP", 4483362458)

-- Highlight Farben:
-- 🔴 Mörder = Rot
-- 🔵 Sheriff = Blau
-- 🟢 Normale Spieler = Grün

local highlights = {}

local function clearHighlights()
   for _, h in pairs(highlights) do
      if h and h.Parent then h:Destroy() end
   end
   highlights = {}
end

local function applyHighlights()
   clearHighlights()
   for _, player in pairs(Players:GetPlayers()) do
      if player ~= LocalPlayer and player.Character then
         local hl = Instance.new("Highlight")
         hl.Adornee = player.Character

         local tool = player.Character:FindFirstChildOfClass("Tool")
         if tool then
            local n = tool.Name:lower()
            if n:find("knife") or n:find("blade") or n:find("murder") then
               -- 🔴 Mörder = Rot
               hl.FillColor = Color3.fromRGB(255, 30, 30)
               hl.OutlineColor = Color3.fromRGB(200, 0, 0)
            elseif n:find("gun") or n:find("sheriff") then
               -- 🔵 Sheriff = Blau
               hl.FillColor = Color3.fromRGB(30, 100, 255)
               hl.OutlineColor = Color3.fromRGB(0, 60, 200)
            else
               -- 🟢 Normale Spieler = Grün
               hl.FillColor = Color3.fromRGB(30, 220, 80)
               hl.OutlineColor = Color3.fromRGB(0, 180, 50)
            end
         else
            -- 🟢 Keine Waffe = Normaler Spieler = Grün
            hl.FillColor = Color3.fromRGB(30, 220, 80)
            hl.OutlineColor = Color3.fromRGB(0, 180, 50)
         end

         hl.FillTransparency = 0.4
         hl.OutlineTransparency = 0
         hl.Parent = player.Character
         table.insert(highlights, hl)
      end
   end
end

ESPTab:CreateToggle({
   Name = "Player ESP (Rot/Blau/Grün)",
   CurrentValue = false,
   Flag = "PlayerESP",
   Callback = function(state)
      if state then
         applyHighlights()
         RunService.Heartbeat:Connect(function()
            if not state then clearHighlights(); return end
            applyHighlights()
         end)
      else
         clearHighlights()
      end
   end,
})

ESPTab:CreateToggle({
   Name = "Nametags",
   CurrentValue = false,
   Flag = "Nametags",
   Callback = function(state)
      RunService.RenderStepped:Connect(function()
         if not state then return end
         for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
               local head = player.Character:FindFirstChild("Head")
               if head and not head:FindFirstChild("CoreXName") then
                  local bb = Instance.new("BillboardGui"); bb.Name = "CoreXName"
                  bb.Size = UDim2.new(0,120,0,30); bb.StudsOffset = Vector3.new(0,3,0)
                  bb.AlwaysOnTop = true; bb.Parent = head
                  local lbl = Instance.new("TextLabel")
                  lbl.Size = UDim2.new(1,0,1,0); lbl.BackgroundTransparency = 1
                  lbl.Text = player.Name; lbl.TextColor3 = Color3.fromRGB(255,255,255)
                  lbl.TextSize = 14; lbl.Font = Enum.Font.GothamBold
                  lbl.TextStrokeTransparency = 0; lbl.Parent = bb
               end
            end
         end
      end)
   end,
})

ESPTab:CreateToggle({
   Name = "Distanz anzeigen",
   CurrentValue = false,
   Flag = "DistESP",
   Callback = function(state)
      RunService.RenderStepped:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if not char then return end
         local hrp = char:FindFirstChild("HumanoidRootPart")
         if not hrp then return end
         for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
               local tHrp = player.Character:FindFirstChild("HumanoidRootPart")
               local head = player.Character:FindFirstChild("Head")
               if tHrp and head then
                  local dist = math.floor((hrp.Position - tHrp.Position).Magnitude)
                  local bb = head:FindFirstChild("CoreXDist")
                  if not bb then
                     bb = Instance.new("BillboardGui"); bb.Name = "CoreXDist"
                     bb.Size = UDim2.new(0,80,0,20); bb.StudsOffset = Vector3.new(0,5,0)
                     bb.AlwaysOnTop = true; bb.Parent = head
                     local dl = Instance.new("TextLabel"); dl.Name = "DistLabel"
                     dl.Size = UDim2.new(1,0,1,0); dl.BackgroundTransparency = 1
                     dl.TextColor3 = Color3.fromRGB(255,220,50); dl.TextSize = 12
                     dl.Font = Enum.Font.GothamBold; dl.TextStrokeTransparency = 0; dl.Parent = bb
                  end
                  local dl = bb:FindFirstChild("DistLabel")
                  if dl then dl.Text = dist.."m" end
               end
            end
         end
      end)
   end,
})

ESPTab:CreateToggle({
   Name = "Health ESP",
   CurrentValue = false,
   Flag = "HealthESP",
   Callback = function(state)
      RunService.RenderStepped:Connect(function()
         if not state then return end
         for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
               local head = player.Character:FindFirstChild("Head")
               local hum = player.Character:FindFirstChildOfClass("Humanoid")
               if head and hum then
                  local bb = head:FindFirstChild("CoreXHP")
                  if not bb then
                     bb = Instance.new("BillboardGui"); bb.Name = "CoreXHP"
                     bb.Size = UDim2.new(0,80,0,18); bb.StudsOffset = Vector3.new(0,1,0)
                     bb.AlwaysOnTop = true; bb.Parent = head
                     local hl2 = Instance.new("TextLabel"); hl2.Name = "HPLabel"
                     hl2.Size = UDim2.new(1,0,1,0); hl2.BackgroundTransparency = 1
                     hl2.TextSize = 12; hl2.Font = Enum.Font.GothamBold
                     hl2.TextStrokeTransparency = 0; hl2.Parent = bb
                  end
                  local hl2 = bb:FindFirstChild("HPLabel")
                  if hl2 then
                     local hp = math.floor(hum.Health)
                     hl2.Text = "❤️ "..hp
                     hl2.TextColor3 = hp > 50 and Color3.fromRGB(50,255,50) or Color3.fromRGB(255,50,50)
                  end
               end
            end
         end
      end)
   end,
})

-- ════════════════════════════════
-- PLAYER TAB
-- ════════════════════════════════
local PlayerTab = Window:CreateTab("👤 Player", 4483362458)

PlayerTab:CreateSlider({
   Name = "Max Health",
   Range = {50, 500},
   Increment = 10,
   Suffix = "HP",
   CurrentValue = 100,
   Flag = "MaxHP",
   Callback = function(val)
      local char = LocalPlayer.Character
      if char then
         local hum = char:FindFirstChildOfClass("Humanoid")
         if hum then hum.MaxHealth = val; hum.Health = val end
      end
   end,
})

PlayerTab:CreateToggle({
   Name = "Auto Heal",
   CurrentValue = false,
   Flag = "AutoHeal",
   Callback = function(state)
      RunService.Heartbeat:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health < hum.MaxHealth then
               hum.Health = math.min(hum.Health + 0.5, hum.MaxHealth)
            end
         end
      end)
   end,
})

PlayerTab:CreateToggle({
   Name = "God Mode",
   CurrentValue = false,
   Flag = "GodMode",
   Callback = function(state)
      local char = LocalPlayer.Character
      if char then
         local hum = char:FindFirstChildOfClass("Humanoid")
         if hum then
            hum:GetPropertyChangedSignal("Health"):Connect(function()
               if state then hum.Health = hum.MaxHealth end
            end)
         end
      end
   end,
})

PlayerTab:CreateToggle({
   Name = "Anti Ragdoll",
   CurrentValue = false,
   Flag = "AntiRagdoll",
   Callback = function(state)
      RunService.Stepped:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false) end
         end
      end)
   end,
})

PlayerTab:CreateSlider({
   Name = "FOV",
   Range = {50, 120},
   Increment = 1,
   Suffix = "°",
   CurrentValue = 70,
   Flag = "FOV",
   Callback = function(val)
      workspace.CurrentCamera.FieldOfView = val
   end,
})

PlayerTab:CreateToggle({
   Name = "Unsichtbar",
   CurrentValue = false,
   Flag = "Invis",
   Callback = function(state)
      local char = LocalPlayer.Character
      if char then
         for _, p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") or p:IsA("Decal") then
               p.Transparency = state and 1 or 0
            end
         end
      end
   end,
})

PlayerTab:CreateToggle({
   Name = "Rainbow Character",
   CurrentValue = false,
   Flag = "Rainbow",
   Callback = function(state)
      RunService.Heartbeat:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if not char then return end
         local t = tick()
         for _, p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") then
               p.Color = Color3.fromHSV((t*0.5)%1, 1, 1)
            end
         end
      end)
   end,
})

PlayerTab:CreateToggle({
   Name = "Spin Charakter",
   CurrentValue = false,
   Flag = "Spin",
   Callback = function(state)
      RunService.Heartbeat:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if not char then return end
         local hrp = char:FindFirstChild("HumanoidRootPart")
         if hrp then hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(5), 0) end
      end)
   end,
})

-- ════════════════════════════════
-- MISC TAB
-- ════════════════════════════════
local MiscTab = Window:CreateTab("⚙️ Misc", 4483362458)

MiscTab:CreateToggle({
   Name = "Fullbright",
   CurrentValue = false,
   Flag = "Fullbright",
   Callback = function(state)
      game:GetService("Lighting").Brightness = state and 6 or 1
      game:GetService("Lighting").FogEnd = state and 9e9 or 100000
   end,
})

MiscTab:CreateToggle({
   Name = "Anti AFK",
   CurrentValue = false,
   Flag = "AntiAFK",
   Callback = function(state)
      if state then
         local VU = game:GetService("VirtualUser")
         LocalPlayer.Idled:Connect(function()
            VU:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            wait(1); VU:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
         end)
      end
   end,
})

MiscTab:CreateToggle({
   Name = "Auto Coins sammeln",
   CurrentValue = false,
   Flag = "AutoCoins",
   Callback = function(state)
      RunService.Heartbeat:Connect(function()
         if not state then return end
         local char = LocalPlayer.Character
         if not char then return end
         local hrp = char:FindFirstChild("HumanoidRootPart")
         if not hrp then return end
         for _, obj in pairs(workspace:GetDescendants()) do
            if (obj.Name == "Coin" or obj.Name == "CoinDrop") and obj:IsA("BasePart") then
               if (hrp.Position - obj.Position).Magnitude < 60 then
                  hrp.CFrame = CFrame.new(obj.Position)
               end
            end
         end
      end)
   end,
})

MiscTab:CreateToggle({
   Name = "Sounds aus",
   CurrentValue = false,
   Flag = "NoSound",
   Callback = function(state)
      for _, s in pairs(workspace:GetDescendants()) do
         if s:IsA("Sound") then s.Volume = state and 0 or 0.5 end
      end
   end,
})

MiscTab:CreateSlider({
   Name = "Fog Dichte",
   Range = {0, 1000},
   Increment = 10,
   Suffix = "",
   CurrentValue = 1000,
   Flag = "Fog",
   Callback = function(val)
      game:GetService("Lighting").FogEnd = val * 100
   end,
})

MiscTab:CreateButton({
   Name = "Teleport zu Spawn",
   Callback = function()
      local char = LocalPlayer.Character
      if char then
         local hrp = char:FindFirstChild("HumanoidRootPart")
         local spawn = workspace:FindFirstChild("SpawnLocation")
         if hrp and spawn then hrp.CFrame = spawn.CFrame + Vector3.new(0,5,0) end
      end
   end,
})

MiscTab:CreateButton({
   Name = "Charakter neu laden",
   Callback = function()
      LocalPlayer:LoadCharacter()
   end,
})

Rayfield:LoadConfiguration()
print("[CoreX | Mörder Mystery] ✅ Geladen!")
