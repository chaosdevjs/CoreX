-- =============================================
--           CoreX Script Hub
--   discord.gg/QzNApvwzgV  |  CoreX Scripts
-- =============================================

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- Key System
local function GetKey()
    local ok, result = pcall(function()
        return game:HttpGet("https://pastebin.com/raw/J91SXgzW")
    end)
    if ok then return result:gsub("%s+", "") end
    return ""
end

local Window = Rayfield:CreateWindow({
    Name = "CoreX Scripts",
    Icon = 0,
    LoadingTitle = "CoreX Scripts",
    LoadingSubtitle = "discord.gg/QzNApvwzgV",
    Theme = "Default",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = { Enabled = true, FolderName = "CoreXConfig", FileName = "CoreX" },
    Discord = { Enabled = true, Invite = "QzNApvwzgV", RememberJoins = true },
    KeySystem = true,
    KeySettings = {
        Title = "CoreX", Subtitle = "Key System",
        Note = "Get your key at discord.gg/QzNApvwzgV",
        FileName = "CoreXKey", SaveKey = true,
        GrabKeyFromSite = false, Key = {GetKey()}
    }
})

-- =============================================
--              VARIABLES
-- =============================================
local AimbotEnabled = false
local AimbotFOV = 263
local AimbotPart = "Head"
local AimbotSmoothing = 5
local AimbotWallCheck = false
local AimbotTeamCheck = false
local AimbotKnockedCheck = false
local ShowFOVCircle = false
local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Radius = AimbotFOV
FOVCircle.Color = Color3.fromRGB(255,255,255)
FOVCircle.Thickness = 1
FOVCircle.Filled = false

local SilentAimEnabled = false
local FlyEnabled = false
local FlySpeed = 70
local NoClipEnabled = false
local SpinbotEnabled = false
local InfJumpEnabled = false
local WalkspeedBoost = 16
local ESPEnabled = false
local FullbrightEnabled = false
local XRayEnabled = false
local GhostModeEnabled = false
local PlayerTrailEnabled = false

-- =============================================
--              FUNCTIONS
-- =============================================

-- Get closest player in FOV
local function GetClosestPlayer()
    local closest = nil
    local shortestDist = AimbotFOV
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            local hum = player.Character:FindFirstChildOfClass("Humanoid")
            if hrp and hum and hum.Health > 0 then
                if AimbotTeamCheck and player.Team == LocalPlayer.Team then continue end
                if AimbotKnockedCheck and hum.Health <= 0 then continue end
                if AimbotWallCheck then
                    local ray = Ray.new(Camera.CFrame.Position, (hrp.Position - Camera.CFrame.Position).Unit * 1000)
                    local hit = Workspace:FindPartOnRayWithIgnoreList(ray, {LocalPlayer.Character})
                    if hit and not hit:IsDescendantOf(player.Character) then continue end
                end
                local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                if onScreen then
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - (Camera.ViewportSize / 2)).Magnitude
                    if dist < shortestDist then
                        shortestDist = dist
                        closest = player
                    end
                end
            end
        end
    end
    return closest
end

-- Fly Logic
local FlyPart = nil
local FlyConnection = nil
local function StartFly()
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not hum then return end
    hum.PlatformStand = true
    FlyPart = Instance.new("BodyVelocity", hrp)
    FlyPart.Velocity = Vector3.zero
    FlyPart.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    local BG = Instance.new("BodyGyro", hrp)
    BG.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    BG.P = 1e4
    FlyConnection = RunService.Heartbeat:Connect(function()
        if not FlyEnabled then
            FlyPart:Destroy()
            BG:Destroy()
            hum.PlatformStand = false
            FlyConnection:Disconnect()
            return
        end
        local dir = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0,1,0) end
        FlyPart.Velocity = dir * FlySpeed
        BG.CFrame = Camera.CFrame
    end)
end

local function StopFly()
    FlyEnabled = false
end

-- ESP
local ESPObjects = {}
local function CreateESP(player)
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "CoreXESP"
    billboard.Size = UDim2.new(0, 100, 0, 40)
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.AlwaysOnTop = true
    local nameLabel = Instance.new("TextLabel", billboard)
    nameLabel.Size = UDim2.new(1, 0, 0.5, 0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    nameLabel.TextScaled = true
    nameLabel.Text = player.Name
    nameLabel.Name = "NameLabel"
    local healthLabel = Instance.new("TextLabel", billboard)
    healthLabel.Size = UDim2.new(1, 0, 0.5, 0)
    healthLabel.Position = UDim2.new(0, 0, 0.5, 0)
    healthLabel.BackgroundTransparency = 1
    healthLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    healthLabel.TextScaled = true
    healthLabel.Name = "HealthLabel"
    ESPObjects[player] = billboard
    return billboard
end

local function UpdateESP()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            local hum = player.Character:FindFirstChildOfClass("Humanoid")
            if hrp and hum then
                if not ESPObjects[player] then
                    CreateESP(player)
                end
                local esp = ESPObjects[player]
                esp.Parent = hrp
                esp.Enabled = ESPEnabled
                local health = esp:FindFirstChild("HealthLabel")
                if health then
                    health.Text = "HP: " .. math.floor(hum.Health)
                end
            end
        end
    end
end

-- NoClip
local NoClipConnection = nil
local function StartNoClip()
    NoClipConnection = RunService.Stepped:Connect(function()
        if NoClipEnabled and LocalPlayer.Character then
            for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end)
end
StartNoClip()

-- InfJump
UserInputService.JumpRequest:Connect(function()
    if InfJumpEnabled and LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

-- Fullbright
local Lighting = game:GetService("Lighting")
local OriginalBrightness = Lighting.Brightness
local OriginalAmbient = Lighting.Ambient
local function SetFullbright(v)
    if v then
        Lighting.Brightness = 2
        Lighting.Ambient = Color3.fromRGB(178,178,178)
        Lighting.OutdoorAmbient = Color3.fromRGB(178,178,178)
    else
        Lighting.Brightness = OriginalBrightness
        Lighting.Ambient = OriginalAmbient
    end
end

-- Spinbot
local SpinConnection = nil
local function StartSpinbot()
    SpinConnection = RunService.Heartbeat:Connect(function()
        if SpinbotEnabled and LocalPlayer.Character then
            local hrp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(10), 0)
            end
        end
    end)
end
StartSpinbot()

-- Ghost Mode
local function SetGhostMode(v)
    if LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.LocalTransparencyModifier = v and 0.7 or 0
            end
        end
    end
end

-- =============================================
--              MAIN LOOP
-- =============================================
RunService.RenderStepped:Connect(function()
    -- FOV Circle
    if ShowFOVCircle then
        FOVCircle.Visible = true
        FOVCircle.Position = Camera.ViewportSize / 2
        FOVCircle.Radius = AimbotFOV
    else
        FOVCircle.Visible = false
    end

    -- Aimbot
    if AimbotEnabled then
        local target = GetClosestPlayer()
        if target and target.Character then
            local part = target.Character:FindFirstChild(AimbotPart) or target.Character:FindFirstChild("HumanoidRootPart")
            if part then
                local targetCFrame = CFrame.new(Camera.CFrame.Position, part.Position)
                Camera.CFrame = Camera.CFrame:Lerp(targetCFrame, 1 / AimbotSmoothing)
            end
        end
    end

    -- ESP Update
    if ESPEnabled then UpdateESP() end

    -- Walkspeed
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = WalkspeedBoost
        end
    end
end)

-- =============================================
--                  MAIN TAB
-- =============================================
local MainTab = Window:CreateTab("Main", 4483345998)
MainTab:CreateSection("CoreX / Emergency Hamburg")
MainTab:CreateParagraph({ Title = "Game Information", Content = "Game Name: Emergency Hamburg\nGame ID: 2992873140" })
MainTab:CreateParagraph({ Title = "CoreX Discord", Content = "discord.gg/QzNApvwzgV" })
MainTab:CreateButton({ Name = "Join Discord", Callback = function()
    setclipboard("https://discord.gg/QzNApvwzgV")
    Rayfield:Notify({ Title = "CoreX", Content = "Discord link copied!", Duration = 3 })
end })

-- =============================================
--                AIMBOT TAB
-- =============================================
local AimbotTab = Window:CreateTab("Aimbot", 4483345998)
AimbotTab:CreateSection("Main Settings")
AimbotTab:CreateToggle({ Name = "Enable Aimbot", CurrentValue = false, Flag = "AimbotEnabled", Callback = function(v) AimbotEnabled = v end })
AimbotTab:CreateSection("Visibility & Checks")
AimbotTab:CreateToggle({ Name = "Wall Check", CurrentValue = false, Flag = "AimbotWallCheck", Callback = function(v) AimbotWallCheck = v end })
AimbotTab:CreateToggle({ Name = "Team Check", CurrentValue = false, Flag = "AimbotTeamCheck", Callback = function(v) AimbotTeamCheck = v end })
AimbotTab:CreateToggle({ Name = "Knocked Check", CurrentValue = false, Flag = "AimbotKnockedCheck", Callback = function(v) AimbotKnockedCheck = v end })
AimbotTab:CreateSection("FOV Settings")
AimbotTab:CreateToggle({ Name = "Show FOV Circle", CurrentValue = false, Flag = "ShowFOV", Callback = function(v) ShowFOVCircle = v end })
AimbotTab:CreateSlider({ Name = "FOV Radius", Range = {0,500}, Increment = 1, Suffix = "px", CurrentValue = 263, Flag = "AimbotFOV", Callback = function(v) AimbotFOV = v end })
AimbotTab:CreateSection("Targeting")
AimbotTab:CreateDropdown({ Name = "Target Part", Options = {"Head","HumanoidRootPart","Torso","LeftArm","RightArm"}, CurrentOption = {"Head"}, Flag = "AimbotPart", Callback = function(v) AimbotPart = v[1] end })
AimbotTab:CreateToggle({ Name = "Prediction", CurrentValue = false, Flag = "AimbotPred", Callback = function(v) end })
AimbotTab:CreateSlider({ Name = "Smoothing", Range = {1,20}, Increment = 1, Suffix = "", CurrentValue = 5, Flag = "AimbotSmooth", Callback = function(v) AimbotSmoothing = v end })

-- =============================================
--              SILENT AIM TAB
-- =============================================
local SilentAimTab = Window:CreateTab("Silent Aim", 4483345998)
SilentAimTab:CreateSection("Silent Aim")
SilentAimTab:CreateToggle({ Name = "Silent Aim", CurrentValue = false, Flag = "SilentAim", Callback = function(v) SilentAimEnabled = v end })
SilentAimTab:CreateSlider({ Name = "FOV Size", Range = {0,500}, Increment = 1, Suffix = "", CurrentValue = 100, Flag = "SilentFOV", Callback = function(v) end })
SilentAimTab:CreateSection("Checks")
SilentAimTab:CreateToggle({ Name = "Knocked Check", CurrentValue = false, Flag = "SilentKnocked", Callback = function(v) end })
SilentAimTab:CreateToggle({ Name = "Team Check", CurrentValue = false, Flag = "SilentTeam", Callback = function(v) end })
SilentAimTab:CreateToggle({ Name = "Wall Check", CurrentValue = false, Flag = "SilentWall", Callback = function(v) end })
SilentAimTab:CreateSection("Targeting")
SilentAimTab:CreateDropdown({ Name = "Hit Parts", Options = {"Head","HumanoidRootPart","Torso","LeftArm","RightArm"}, CurrentOption = {"Head"}, Flag = "SilentPart", Callback = function(v) end })

-- =============================================
--               GUN MODS TAB
-- =============================================
local GunModsTab = Window:CreateTab("Gun Mods", 4483345998)
GunModsTab:CreateSection("Gun Mods")
GunModsTab:CreateToggle({ Name = "Auto Reload", CurrentValue = false, Flag = "AutoReload", Callback = function(v)
    if v then
        RunService.Heartbeat:Connect(function()
            if v and LocalPlayer.Character then
                local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
                if tool then
                    local fire = tool:FindFirstChild("Fire") or tool:FindFirstChildOfClass("RemoteEvent")
                    pcall(function() tool:Activate() end)
                end
            end
        end)
    end
end })
GunModsTab:CreateToggle({ Name = "No Recoil", CurrentValue = false, Flag = "NoRecoil", Callback = function(v)
    if v then
        RunService.RenderStepped:Connect(function()
            if v then Camera.CFrame = Camera.CFrame end
        end)
    end
end })
GunModsTab:CreateDropdown({ Name = "Gun Sound", Options = {"Default","Silent","Loud"}, CurrentOption = {"Default"}, Flag = "GunSound", Callback = function(v) end })
GunModsTab:CreateSlider({ Name = "Crosshair Size", Range = {1,100}, Increment = 1, Suffix = "Size", CurrentValue = 25, Flag = "CrosshairSize", Callback = function(v) end })

-- =============================================
--               CAR MODS TAB
-- =============================================
local CarModsTab = Window:CreateTab("Car Mods", 4483345998)
CarModsTab:CreateSection("Vehicle Mods")
CarModsTab:CreateToggle({ Name = "Vehicle Godmode", CurrentValue = false, Flag = "VehicleGod", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v and LocalPlayer.Character then
            local vehicle = LocalPlayer.Character:FindFirstChildOfClass("VehicleSeat") or Workspace:FindFirstChild("Vehicle")
            if vehicle then
                pcall(function() vehicle.MaxHealth = math.huge; vehicle.Health = math.huge end)
            end
        end
    end)
end })
CarModsTab:CreateToggle({ Name = "Infinite Fuel", CurrentValue = false, Flag = "InfFuel", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v then
            for _, obj in pairs(Workspace:GetDescendants()) do
                if obj.Name == "Fuel" or obj.Name == "FuelLevel" then
                    pcall(function() obj.Value = 100 end)
                end
            end
        end
    end)
end })
CarModsTab:CreateToggle({ Name = "Anti Crash Damage", CurrentValue = false, Flag = "AntiCrash", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v and LocalPlayer.Character then
            local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.Health = hum.MaxHealth end
        end
    end)
end })
CarModsTab:CreateInput({ Name = "Numberplate Text", PlaceholderText = "CoreX", RemoveTextAfterFocusLost = false, Callback = function(v)
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj.Name == "Numberplate" or obj.Name == "LicensePlate" then
            pcall(function() obj.SurfaceGui.TextLabel.Text = v end)
        end
    end
end })
CarModsTab:CreateSection("CarFly Options")
CarModsTab:CreateToggle({ Name = "Car Fly", CurrentValue = false, Flag = "CarFly", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v and LocalPlayer.Character then
            local seat = LocalPlayer.Character:FindFirstChildOfClass("Humanoid").SeatPart
            if seat then
                local bv = seat:FindFirstChild("CarFlyBV") or Instance.new("BodyVelocity", seat)
                bv.Name = "CarFlyBV"
                bv.MaxForce = Vector3.new(math.huge,math.huge,math.huge)
                local dir = Vector3.zero
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
                bv.Velocity = dir * 100
            end
        end
    end)
end })
CarModsTab:CreateSlider({ Name = "Car Fly Speed", Range = {10,500}, Increment = 1, Suffix = "Speed", CurrentValue = 100, Flag = "CarFlySpeed", Callback = function(v) end })
CarModsTab:CreateSection("Extras")
CarModsTab:CreateButton({ Name = "Jump Out Of Vehicle", Callback = function()
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Sit = false end
    end
end })
CarModsTab:CreateButton({ Name = "Bring Own Car", Callback = function()
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("VehicleSeat") and obj:FindFirstChild("Owner") then
            pcall(function()
                if obj.Owner.Value == LocalPlayer.Name then
                    obj.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0,0,-10)
                end
            end)
        end
    end
end })

-- =============================================
--               VISUALS TAB
-- =============================================
local VisualsTab = Window:CreateTab("Visuals", 4483345998)
VisualsTab:CreateSection("ESP")
VisualsTab:CreateToggle({ Name = "Show Names + Health ESP", CurrentValue = false, Flag = "ESPEnabled", Callback = function(v)
    ESPEnabled = v
    if not v then
        for _, esp in pairs(ESPObjects) do
            if esp then esp.Enabled = false end
        end
    end
end })
VisualsTab:CreateToggle({ Name = "Show Wanted", CurrentValue = false, Flag = "ShowWanted", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v then
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local esp = ESPObjects[player]
                        if esp then
                            local wanted = esp:FindFirstChild("WantedLabel") or Instance.new("TextLabel", esp)
                            wanted.Name = "WantedLabel"
                            wanted.Size = UDim2.new(1,0,0.3,0)
                            wanted.Position = UDim2.new(0,0,0.7,0)
                            wanted.BackgroundTransparency = 1
                            wanted.TextColor3 = Color3.fromRGB(255,0,0)
                            wanted.TextScaled = true
                            wanted.Text = "⭐ WANTED"
                        end
                    end
                end
            end
        end
    end)
end })
VisualsTab:CreateSlider({ Name = "Text Size", Range = {8,40}, Increment = 1, Suffix = "px", CurrentValue = 16, Flag = "TextSize", Callback = function(v) end })
VisualsTab:CreateSlider({ Name = "Max Range", Range = {100,5000}, Increment = 50, Suffix = "studs", CurrentValue = 1400, Flag = "MaxRange", Callback = function(v) end })

-- =============================================
--              MOVEMENT TAB
-- =============================================
local MovementTab = Window:CreateTab("Movement", 4483345998)
MovementTab:CreateSection("Main Options")
MovementTab:CreateToggle({ Name = "NoClip", CurrentValue = false, Flag = "NoClip", Callback = function(v) NoClipEnabled = v end })
MovementTab:CreateInput({ Name = "Walkspeed Boost", PlaceholderText = "16", RemoveTextAfterFocusLost = false, Callback = function(v)
    WalkspeedBoost = tonumber(v) or 16
end })
MovementTab:CreateToggle({ Name = "Spinbot", CurrentValue = false, Flag = "Spinbot", Callback = function(v) SpinbotEnabled = v end })
MovementTab:CreateSection("Fly Settings")
MovementTab:CreateToggle({ Name = "Player Fly", CurrentValue = false, Flag = "PlayerFly", Callback = function(v)
    FlyEnabled = v
    if v then StartFly() end
end })
MovementTab:CreateSlider({ Name = "Fly Speed", Range = {10,300}, Increment = 1, Suffix = "Speed", CurrentValue = 70, Flag = "FlySpeed", Callback = function(v) FlySpeed = v end })
MovementTab:CreateSection("Misc")
MovementTab:CreateToggle({ Name = "Inf Jump", CurrentValue = false, Flag = "InfJump", Callback = function(v) InfJumpEnabled = v end })
MovementTab:CreateButton({ Name = "Infinite Stamina", Callback = function()
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj.Name == "Stamina" or obj.Name == "StaminaValue" then
            pcall(function() obj.Value = 100 end)
        end
    end
end })

-- =============================================
--              GRAPHICS TAB
-- =============================================
local GraphicsTab = Window:CreateTab("Graphics", 4483345998)
GraphicsTab:CreateSection("Graphics")
GraphicsTab:CreateToggle({ Name = "Fullbright", CurrentValue = false, Flag = "Fullbright", Callback = function(v) SetFullbright(v) end })
GraphicsTab:CreateToggle({ Name = "Remove Atmosphere", CurrentValue = false, Flag = "RemoveAtmo", Callback = function(v)
    local atmo = Lighting:FindFirstChildOfClass("Atmosphere")
    if atmo then atmo.Density = v and 0 or 0.395 end
end })
GraphicsTab:CreateSection("Player Options")
GraphicsTab:CreateToggle({ Name = "Ghost Mode", CurrentValue = false, Flag = "GhostMode", Callback = function(v)
    GhostModeEnabled = v
    SetGhostMode(v)
end })
GraphicsTab:CreateToggle({ Name = "Rainbow Ghost Mode", CurrentValue = false, Flag = "RainbowGhost", Callback = function(v)
    if v then
        RunService.Heartbeat:Connect(function()
            if v and LocalPlayer.Character then
                for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.Color = Color3.fromHSV(tick() % 1, 1, 1)
                        part.LocalTransparencyModifier = 0.5
                    end
                end
            end
        end)
    end
end })

-- =============================================
--              TELEPORT TAB
-- =============================================
local TeleportTab = Window:CreateTab("Teleport", 4483345998)
TeleportTab:CreateSection("Main Control")
TeleportTab:CreateButton({ Name = "Nearest Wanted Player", Callback = function()
    local closest, dist = nil, math.huge
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local d = (hrp.Position - LocalPlayer.Character.HumanoidRootPart.Position).Magnitude
                if d < dist then dist = d; closest = hrp end
            end
        end
    end
    if closest and LocalPlayer.Character then
        LocalPlayer.Character.HumanoidRootPart.CFrame = closest.CFrame * CFrame.new(0,0,3)
    end
end })
TeleportTab:CreateSection("Work Places")
TeleportTab:CreateButton({ Name = "Police Station", Callback = function()
    if LocalPlayer.Character then
        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(0, 10, 0))
        Rayfield:Notify({ Title = "Teleport", Content = "Teleported to Police Station!", Duration = 3 })
    end
end })
TeleportTab:CreateButton({ Name = "Teleport to Safe Zone", Callback = function()
    if LocalPlayer.Character then
        LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(0, 10, 0))
        Rayfield:Notify({ Title = "Teleport", Content = "Teleported to Safe Zone!", Duration = 3 })
    end
end })

-- =============================================
--             ANIMATIONS TAB
-- =============================================
local AnimationsTab = Window:CreateTab("Animations", 4483345998)
AnimationsTab:CreateSection("Pre Emotes")
AnimationsTab:CreateToggle({ Name = "Fake Cuffed", CurrentValue = false, Flag = "FakeCuffed", Callback = function(v)
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            local anim = Instance.new("Animation")
            anim.AnimationId = "rbxassetid://5915693440"
            local track = hum:LoadAnimation(anim)
            if v then track:Play() else track:Stop() end
        end
    end
end })
AnimationsTab:CreateToggle({ Name = "Fake Dead", CurrentValue = false, Flag = "FakeDead", Callback = function(v)
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            local anim = Instance.new("Animation")
            anim.AnimationId = "rbxassetid://2987270261"
            local track = hum:LoadAnimation(anim)
            if v then track:Play() else track:Stop() end
        end
    end
end })
AnimationsTab:CreateSection("Custom Animations")
local AnimIds = {
    ["Helicopter"] = "rbxassetid://5222413868",
    ["Default Dance"] = "rbxassetid://507771019",
    ["Slickback"] = "rbxassetid://10921267246",
    ["Sit"] = "rbxassetid://2506281703",
    ["Take The L"] = "rbxassetid://507765644",
    ["Spider"] = "rbxassetid://616010382",
}
local SelectedAnim = "Default Dance"
AnimationsTab:CreateDropdown({ Name = "Select Animation", Options = {"Helicopter","Default Dance","Slickback","Sit","Take The L","Spider"}, CurrentOption = {"Default Dance"}, Flag = "SelectedAnim", Callback = function(v) SelectedAnim = v[1] end })
AnimationsTab:CreateButton({ Name = "Play Animation", Callback = function()
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum and AnimIds[SelectedAnim] then
            local anim = Instance.new("Animation")
            anim.AnimationId = AnimIds[SelectedAnim]
            local track = hum:LoadAnimation(anim)
            track:Play()
        end
    end
end })
AnimationsTab:CreateButton({ Name = "Stop Animation", Callback = function()
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            for _, track in pairs(hum:GetPlayingAnimationTracks()) do
                track:Stop()
            end
        end
    end
end })

-- =============================================
--               POLICE TAB
-- =============================================
local PoliceTab = Window:CreateTab("Police", 4483345998)
PoliceTab:CreateSection("Police AutoFarm")
PoliceTab:CreateToggle({ Name = "RadarFarm", CurrentValue = false, Flag = "RadarFarm", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v then
            for _, obj in pairs(Workspace:GetDescendants()) do
                if obj.Name == "Radar" or obj.Name == "SpeedCamera" then
                    pcall(function() obj:Activate() end)
                end
            end
        end
    end)
end })
PoliceTab:CreateButton({ Name = "Anti AFK", Callback = function()
    local vu = game:GetService("VirtualUser")
    LocalPlayer.Idled:Connect(function()
        vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    end)
    Rayfield:Notify({ Title = "CoreX", Content = "Anti AFK Activated!", Duration = 3 })
end })
PoliceTab:CreateSection("Anti Police")
PoliceTab:CreateToggle({ Name = "Anti Taser", CurrentValue = false, Flag = "AntiTaser", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v and LocalPlayer.Character then
            local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.PlatformStand = false end
        end
    end)
end })
PoliceTab:CreateButton({ Name = "Jump Out of Vehicle [When Cuffed]", Callback = function()
    if LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.Sit = false
            hum.PlatformStand = false
            LocalPlayer.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0,5,0)
        end
    end
end })

-- =============================================
--               BYPASS TAB
-- =============================================
local BypassTab = Window:CreateTab("Bypass", 4483345998)
BypassTab:CreateSection("Anti Cheat")
BypassTab:CreateToggle({ Name = "Anti Cheat Bypass", CurrentValue = false, Flag = "ACBypass", Callback = function(v)
    if v then
        RunService.Heartbeat:Connect(function()
            pcall(function()
                for _, script in pairs(Workspace:GetDescendants()) do
                    if script:IsA("Script") and (script.Name:lower():find("anti") or script.Name:lower():find("cheat")) then
                        script.Disabled = true
                    end
                end
            end)
        end)
    end
end })
BypassTab:CreateParagraph({ Title = "Warning", Content = "This does not fully block the Anti Cheat." })

-- =============================================
--                MISC TAB
-- =============================================
local MiscTab = Window:CreateTab("Misc", 4483345998)
MiscTab:CreateSection("Player")
MiscTab:CreateButton({ Name = "Respawn", Callback = function()
    LocalPlayer.Character:BreakJoints()
end })
MiscTab:CreateButton({ Name = "Self Revive", Callback = function()
    local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.Health = hum.MaxHealth end
end })
MiscTab:CreateButton({ Name = "Leave Game", Callback = function()
    LocalPlayer:Kick("CoreX - Left Game")
end })
MiscTab:CreateButton({ Name = "Remove Signs", Callback = function()
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj.Name:lower():find("sign") or obj.Name:lower():find("schild") then
            obj:Destroy()
        end
    end
end })
MiscTab:CreateButton({ Name = "Remove Trees", Callback = function()
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj.Name:lower():find("tree") or obj.Name:lower():find("baum") then
            obj:Destroy()
        end
    end
end })
MiscTab:CreateToggle({ Name = "Auto Punch", CurrentValue = false, Flag = "AutoPunch", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v and LocalPlayer.Character then
            local tool = LocalPlayer.Character:FindFirstChildOfClass("Tool")
            if tool then pcall(function() tool:Activate() end) end
        end
    end)
end })
MiscTab:CreateSection("Safety")
MiscTab:CreateToggle({ Name = "Anti Fall", CurrentValue = false, Flag = "AntiFall", Callback = function(v)
    RunService.Heartbeat:Connect(function()
        if v and LocalPlayer.Character then
            local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
        end
    end)
end })
MiscTab:CreateSection("Server")
MiscTab:CreateButton({ Name = "Server Hop", Callback = function()
    game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
end })
MiscTab:CreateButton({ Name = "Rejoin", Callback = function()
    game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
end })
MiscTab:CreateToggle({ Name = "Auto Rejoin when kicked", CurrentValue = false, Flag = "AutoRejoin", Callback = function(v)
    if v then
        LocalPlayer.OnTeleport:Connect(function(state)
            if state == Enum.TeleportState.Failed then
                game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer)
            end
        end)
    end
end })
