local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CoreX_MM2"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game.CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 370, 0, 520)
MainFrame.Position = UDim2.new(0.5, -185, 0.5, -260)
MainFrame.BackgroundColor3 = Color3.fromRGB(13, 13, 18)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local ms = Instance.new("UIStroke", MainFrame)
ms.Color = Color3.fromRGB(180, 0, 0); ms.Thickness = 2

-- TITELLEISTE
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 45)
TitleBar.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 12)
local TitleFix = Instance.new("Frame")
TitleFix.Size = UDim2.new(1, 0, 0.5, 0)
TitleFix.Position = UDim2.new(0, 0, 0.5, 0)
TitleFix.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
TitleFix.BorderSizePixel = 0
TitleFix.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -50, 1, 0)
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "🔪 CoreX | Mörder Mystery"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 15
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -38, 0.5, -15)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 30, 30)
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255,255,255)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.BorderSizePixel = 0
CloseBtn.Parent = TitleBar
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 6)
CloseBtn.MouseButton1Click:Connect(function()
    TweenService:Create(MainFrame, TweenInfo.new(0.3), {Size = UDim2.new(0,0,0,0)}):Play()
    wait(0.3); ScreenGui:Destroy()
end)

-- STATUS
local StatusFrame = Instance.new("Frame")
StatusFrame.Size = UDim2.new(1, -20, 0, 28)
StatusFrame.Position = UDim2.new(0, 10, 0, 52)
StatusFrame.BackgroundColor3 = Color3.fromRGB(22, 22, 32)
StatusFrame.BorderSizePixel = 0
StatusFrame.Parent = MainFrame
Instance.new("UICorner", StatusFrame).CornerRadius = UDim.new(0, 6)

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, -10, 1, 0)
StatusLabel.Position = UDim2.new(0, 10, 0, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "● CoreX bereit"
StatusLabel.TextColor3 = Color3.fromRGB(50, 220, 100)
StatusLabel.TextSize = 12
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.Parent = StatusFrame

-- TABS
local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(1, -20, 0, 32)
TabFrame.Position = UDim2.new(0, 10, 0, 88)
TabFrame.BackgroundTransparency = 1
TabFrame.Parent = MainFrame
local TL = Instance.new("UIListLayout", TabFrame)
TL.FillDirection = Enum.FillDirection.Horizontal
TL.Padding = UDim.new(0, 4)

local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, -20, 0, 345)
ContentArea.Position = UDim2.new(0, 10, 0, 128)
ContentArea.BackgroundColor3 = Color3.fromRGB(18, 18, 26)
ContentArea.BorderSizePixel = 0
ContentArea.Parent = MainFrame
Instance.new("UICorner", ContentArea).CornerRadius = UDim.new(0, 8)
local cas = Instance.new("UIStroke", ContentArea)
cas.Color = Color3.fromRGB(45, 45, 65); cas.Thickness = 1

local Footer = Instance.new("TextLabel")
Footer.Size = UDim2.new(1, 0, 0, 20)
Footer.Position = UDim2.new(0, 0, 1, -22)
Footer.BackgroundTransparency = 1
Footer.Text = "CoreX | Mörder Mystery  •  F4 = Toggle"
Footer.TextColor3 = Color3.fromRGB(60, 60, 80)
Footer.TextSize = 11
Footer.Font = Enum.Font.Gotham
Footer.Parent = MainFrame

-- TOGGLE FUNKTION
local function createToggle(parent, yPos, text, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 36)
    frame.Position = UDim2.new(0, 10, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(26, 26, 36)
    frame.BorderSizePixel = 0
    frame.Parent = parent
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 7)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -60, 1, 0)
    lbl.Position = UDim2.new(0, 12, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(200, 200, 215)
    lbl.TextSize = 12
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = frame
    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(0, 40, 0, 20)
    bg.Position = UDim2.new(1, -52, 0.5, -10)
    bg.BackgroundColor3 = Color3.fromRGB(45, 45, 60)
    bg.BorderSizePixel = 0
    bg.Parent = frame
    Instance.new("UICorner", bg).CornerRadius = UDim.new(1, 0)
    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0, 14, 0, 14)
    dot.Position = UDim2.new(0, 3, 0.5, -7)
    dot.BackgroundColor3 = Color3.fromRGB(140, 140, 160)
    dot.BorderSizePixel = 0
    dot.Parent = bg
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
    local enabled = false
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text = ""
    btn.Parent = frame
    btn.MouseButton1Click:Connect(function()
        enabled = not enabled
        if enabled then
            TweenService:Create(bg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(170,0,0)}):Play()
            TweenService:Create(dot, TweenInfo.new(0.2), {Position = UDim2.new(0,22,0.5,-7), BackgroundColor3 = Color3.fromRGB(255,255,255)}):Play()
            lbl.TextColor3 = Color3.fromRGB(255,255,255)
        else
            TweenService:Create(bg, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(45,45,60)}):Play()
            TweenService:Create(dot, TweenInfo.new(0.2), {Position = UDim2.new(0,3,0.5,-7), BackgroundColor3 = Color3.fromRGB(140,140,160)}):Play()
            lbl.TextColor3 = Color3.fromRGB(200,200,215)
        end
        if callback then callback(enabled) end
    end)
end

-- SLIDER FUNKTION
local function createSlider(parent, yPos, text, minVal, maxVal, defaultVal, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 52)
    frame.Position = UDim2.new(0, 10, 0, yPos)
    frame.BackgroundColor3 = Color3.fromRGB(26, 26, 36)
    frame.BorderSizePixel = 0
    frame.Parent = parent
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 7)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -70, 0, 22)
    lbl.Position = UDim2.new(0, 12, 0, 4)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(200, 200, 215)
    lbl.TextSize = 12
    lbl.Font = Enum.Font.Gotham
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.Parent = frame
    local valLbl = Instance.new("TextLabel")
    valLbl.Size = UDim2.new(0, 55, 0, 22)
    valLbl.Position = UDim2.new(1, -67, 0, 4)
    valLbl.BackgroundTransparency = 1
    valLbl.Text = tostring(defaultVal)
    valLbl.TextColor3 = Color3.fromRGB(220, 60, 60)
    valLbl.TextSize = 12
    valLbl.Font = Enum.Font.GothamBold
    valLbl.TextXAlignment = Enum.TextXAlignment.Right
    valLbl.Parent = frame
    local trackBg = Instance.new("Frame")
    trackBg.Size = UDim2.new(1, -24, 0, 6)
    trackBg.Position = UDim2.new(0, 12, 0, 34)
    trackBg.BackgroundColor3 = Color3.fromRGB(45, 45, 65)
    trackBg.BorderSizePixel = 0
    trackBg.Parent = frame
    Instance.new("UICorner", trackBg).CornerRadius = UDim.new(1, 0)
    local pct = (defaultVal - minVal) / (maxVal - minVal)
    local fill = Instance.new("Frame")
    fill.Size = UDim2.new(pct, 0, 1, 0)
    fill.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
    fill.BorderSizePixel = 0
    fill.Parent = trackBg
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)
    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 14, 0, 14)
    knob.Position = UDim2.new(pct, -7, 0.5, -7)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.BorderSizePixel = 0
    knob.Parent = trackBg
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
    local dragging = false
    local clickArea = Instance.new("TextButton")
    clickArea.Size = UDim2.new(1, 0, 4, 0)
    clickArea.Position = UDim2.new(0, 0, 0.5, -15)
    clickArea.BackgroundTransparency = 1
    clickArea.Text = ""
    clickArea.Parent = trackBg
    local function updateSlider(inputX)
        local rel = math.clamp((inputX - trackBg.AbsolutePosition.X) / trackBg.AbsoluteSize.X, 0, 1)
        local value = math.floor(minVal + (maxVal - minVal) * rel)
        fill.Size = UDim2.new(rel, 0, 1, 0)
        knob.Position = UDim2.new(rel, -7, 0.5, -7)
        valLbl.Text = tostring(value)
        if callback then callback(value) end
    end
    clickArea.MouseButton1Down:Connect(function()
        dragging = true; updateSlider(UserInputService:GetMouseLocation().X)
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            updateSlider(input.Position.X)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
end

-- ════════════════════════════════
-- SPEED TAB
-- ════════════════════════════════
local speedContent = Instance.new("ScrollingFrame")
speedContent.Size = UDim2.new(1, 0, 1, 0)
speedContent.BackgroundTransparency = 1
speedContent.BorderSizePixel = 0
speedContent.ScrollBarThickness = 3
speedContent.ScrollBarImageColor3 = Color3.fromRGB(180,0,0)
speedContent.CanvasSize = UDim2.new(0,0,0,560)
speedContent.Visible = false
speedContent.Parent = ContentArea

createSlider(speedContent, 10, "⚡ Walk Speed", 16, 150, 16, function(val)
    local char = LocalPlayer.Character
    if char then local h = char:FindFirstChildOfClass("Humanoid") if h then h.WalkSpeed = val end end
    StatusLabel.Text = "● Speed: "..val; StatusLabel.TextColor3 = Color3.fromRGB(255,200,50)
end)
createSlider(speedContent, 72, "🦘 Jump Power", 50, 400, 50, function(val)
    local char = LocalPlayer.Character
    if char then local h = char:FindFirstChildOfClass("Humanoid") if h then h.JumpPower = val end end
    StatusLabel.Text = "● Jump: "..val; StatusLabel.TextColor3 = Color3.fromRGB(100,180,255)
end)
createSlider(speedContent, 134, "🌍 Gravity", 10, 196, 196, function(val)
    workspace.Gravity = val
    StatusLabel.Text = "● Gravity: "..val; StatusLabel.TextColor3 = Color3.fromRGB(180,100,255)
end)
createToggle(speedContent, 196, "♾️ Infinite Jump", function(state)
    UserInputService.JumpRequest:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if char then local h = char:FindFirstChildOfClass("Humanoid") if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end end
    end)
end)
createToggle(speedContent, 240, "👻 No Clip", function(state)
    RunService.Stepped:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if char then for _, p in pairs(char:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = false end end end
    end)
end)
createToggle(speedContent, 284, "✈️ Fly Modus (WASD)", function(state)
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if state then
        local bg2 = Instance.new("BodyGyro"); bg2.MaxTorque = Vector3.new(9e9,9e9,9e9); bg2.P = 9e4; bg2.Parent = hrp
        local bv = Instance.new("BodyVelocity"); bv.Velocity = Vector3.zero; bv.MaxForce = Vector3.new(9e9,9e9,9e9); bv.Parent = hrp
        RunService.Heartbeat:Connect(function()
            if not state then bg2:Destroy(); bv:Destroy(); return end
            local cam = workspace.CurrentCamera; local spd = 50; local vel = Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then vel += cam.CFrame.LookVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then vel -= cam.CFrame.LookVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then vel -= cam.CFrame.RightVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then vel += cam.CFrame.RightVector*spd end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then vel += Vector3.new(0,spd,0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then vel -= Vector3.new(0,spd,0) end
            bv.Velocity = vel; bg2.CFrame = cam.CFrame
        end)
    end
end)
createToggle(speedContent, 328, "🔄 Auto Respawn", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health <= 0 then wait(1); LocalPlayer:LoadCharacter() end
        end
    end)
end)
createToggle(speedContent, 372, "🧲 Teleport zu Spawn", function(state)
    if state then
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local spawn = workspace:FindFirstChild("SpawnLocation")
            if hrp and spawn then hrp.CFrame = spawn.CFrame + Vector3.new(0,5,0) end
        end
    end
end)
createToggle(speedContent, 416, "🏃 Speed Toggle (E)", function(state)
    UserInputService.InputBegan:Connect(function(input, gp)
        if gp then return end
        if input.KeyCode == Enum.KeyCode.E and state then
            local char = LocalPlayer.Character
            if char then
                local h = char:FindFirstChildOfClass("Humanoid")
                if h then h.WalkSpeed = h.WalkSpeed == 16 and 80 or 16 end
            end
        end
    end)
end)

-- ════════════════════════════════
-- ESP TAB
-- ════════════════════════════════
local espContent = Instance.new("ScrollingFrame")
espContent.Size = UDim2.new(1, 0, 1, 0)
espContent.BackgroundTransparency = 1
espContent.BorderSizePixel = 0
espContent.ScrollBarThickness = 3
espContent.ScrollBarImageColor3 = Color3.fromRGB(180,0,0)
espContent.CanvasSize = UDim2.new(0,0,0,500)
espContent.Visible = false
espContent.Parent = ContentArea

local highlights = {}
createToggle(espContent, 10, "👁 Player Highlight", function(state)
    for _, h in pairs(highlights) do if h and h.Parent then h:Destroy() end end
    highlights = {}
    StatusLabel.Text = state and "● ESP: Aktiv" or "● CoreX bereit"
    StatusLabel.TextColor3 = state and Color3.fromRGB(255,100,100) or Color3.fromRGB(50,220,100)
    if state then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local hl = Instance.new("Highlight")
                hl.Adornee = player.Character
                hl.FillColor = Color3.fromRGB(255,50,50)
                hl.OutlineColor = Color3.fromRGB(255,0,0)
                hl.FillTransparency = 0.5
                hl.Parent = player.Character
                table.insert(highlights, hl)
            end
        end
    end
end)
createToggle(espContent, 54, "🔵 Sheriff ESP", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool and (tool.Name:lower():find("gun") or tool.Name:lower():find("sheriff")) then
                    if not player.Character:FindFirstChild("SheriffHL") then
                        local hl = Instance.new("Highlight"); hl.Name = "SheriffHL"
                        hl.Adornee = player.Character
                        hl.FillColor = Color3.fromRGB(50,200,255)
                        hl.OutlineColor = Color3.fromRGB(0,150,255)
                        hl.FillTransparency = 0.4; hl.Parent = player.Character
                    end
                end
            end
        end
    end)
end)
createToggle(espContent, 98, "🔴 Mörder ESP", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool and (tool.Name:lower():find("knife") or tool.Name:lower():find("blade")) then
                    if not player.Character:FindFirstChild("KillerHL") then
                        local hl = Instance.new("Highlight"); hl.Name = "KillerHL"
                        hl.Adornee = player.Character
                        hl.FillColor = Color3.fromRGB(255,30,30)
                        hl.OutlineColor = Color3.fromRGB(200,0,0)
                        hl.FillTransparency = 0.3; hl.Parent = player.Character
                    end
                end
            end
        end
    end)
end)
createToggle(espContent, 142, "📛 Nametags", function(state)
    RunService.RenderStepped:Connect(function()
        if not state then return end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local head = player.Character:FindFirstChild("Head")
                if head and not head:FindFirstChild("CoreXName") then
                    local bb = Instance.new("BillboardGui"); bb.Name = "CoreXName"
                    bb.Size = UDim2.new(0,100,0,30); bb.StudsOffset = Vector3.new(0,2.5,0)
                    bb.AlwaysOnTop = true; bb.Parent = head
                    local lbl2 = Instance.new("TextLabel")
                    lbl2.Size = UDim2.new(1,0,1,0); lbl2.BackgroundTransparency = 1
                    lbl2.Text = player.Name; lbl2.TextColor3 = Color3.fromRGB(255,255,255)
                    lbl2.TextSize = 14; lbl2.Font = Enum.Font.GothamBold
                    lbl2.TextStrokeTransparency = 0; lbl2.Parent = bb
                end
            end
        end
    end)
end)
createToggle(espContent, 186, "📏 Distanz ESP", function(state)
    RunService.RenderStepped:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local tHrp = player.Character:FindFirstChild("HumanoidRootPart")
                local head = player.Character:FindFirstChild("Head")
                if tHrp and head then
                    local dist = math.floor((hrp.Position - tHrp.Position).Magnitude)
                    local bb = head:FindFirstChild("CoreXDist")
                    if not bb then
                        bb = Instance.new("BillboardGui"); bb.Name = "CoreXDist"
                        bb.Size = UDim2.new(0,80,0,20); bb.StudsOffset = Vector3.new(0,4,0)
                        bb.AlwaysOnTop = true; bb.Parent = head
                        local dl = Instance.new("TextLabel"); dl.Name = "DistLabel"
                        dl.Size = UDim2.new(1,0,1,0); dl.BackgroundTransparency = 1
                        dl.TextColor3 = Color3.fromRGB(255,220,50); dl.TextSize = 12
                        dl.Font = Enum.Font.GothamBold; dl.TextStrokeTransparency = 0; dl.Parent = bb
                    end
                    local dl = bb:FindFirstChild("DistLabel")
                    if dl then dl.Text = dist.."m" end
                end
            end
        end
    end)
end)
createToggle(espContent, 230, "❤️ Health ESP", function(state)
    RunService.RenderStepped:Connect(function()
        if not state then return end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local head = player.Character:FindFirstChild("Head")
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if head and hum then
                    local bb = head:FindFirstChild("CoreXHP")
                    if not bb then
                        bb = Instance.new("BillboardGui"); bb.Name = "CoreXHP"
                        bb.Size = UDim2.new(0,80,0,16); bb.StudsOffset = Vector3.new(0,1,0)
                        bb.AlwaysOnTop = true; bb.Parent = head
                        local hl2 = Instance.new("TextLabel"); hl2.Name = "HPLabel"
                        hl2.Size = UDim2.new(1,0,1,0); hl2.BackgroundTransparency = 1
                        hl2.TextSize = 11; hl2.Font = Enum.Font.GothamBold
                        hl2.TextStrokeTransparency = 0; hl2.Parent = bb
                    end
                    local hl2 = bb:FindFirstChild("HPLabel")
                    if hl2 then
                        local hp = math.floor(hum.Health)
                        hl2.Text = "❤️ "..hp
                        hl2.TextColor3 = hp > 50 and Color3.fromRGB(50,255,50) or Color3.fromRGB(255,50,50)
                    end
                end
            end
        end
    end)
end)
createToggle(espContent, 274, "🌐 Chams (Durch Wände)", function(state)
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            for _, part in pairs(player.Character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CastShadow = not state
                end
            end
        end
    end
end)

-- ════════════════════════════════
-- MISC TAB
-- ════════════════════════════════
local miscContent = Instance.new("ScrollingFrame")
miscContent.Size = UDim2.new(1, 0, 1, 0)
miscContent.BackgroundTransparency = 1
miscContent.BorderSizePixel = 0
miscContent.ScrollBarThickness = 3
miscContent.ScrollBarImageColor3 = Color3.fromRGB(180,0,0)
miscContent.CanvasSize = UDim2.new(0,0,0,600)
miscContent.Visible = false
miscContent.Parent = ContentArea

createToggle(miscContent, 10, "🌙 Fullbright", function(state)
    game:GetService("Lighting").Brightness = state and 6 or 1
    game:GetService("Lighting").FogEnd = state and 9e9 or 100000
    StatusLabel.Text = state and "● Fullbright AN" or "● CoreX bereit"
    StatusLabel.TextColor3 = state and Color3.fromRGB(255,230,100) or Color3.fromRGB(50,220,100)
end)
createToggle(miscContent, 54, "🙈 Unsichtbar", function(state)
    local char = LocalPlayer.Character
    if char then
        for _, p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") or p:IsA("Decal") then p.Transparency = state and 1 or 0 end
        end
    end
end)
createToggle(miscContent, 98, "🛡 Anti AFK", function(state)
    if state then
        local VU = game:GetService("VirtualUser")
        LocalPlayer.Idled:Connect(function()
            VU:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            wait(1); VU:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        end)
    end
end)
createToggle(miscContent, 142, "💰 Auto Coins", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        for _, obj in pairs(workspace:GetDescendants()) do
            if (obj.Name == "Coin" or obj.Name == "CoinDrop") and obj:IsA("BasePart") then
                if (hrp.Position - obj.Position).Magnitude < 60 then
                    hrp.CFrame = CFrame.new(obj.Position)
                end
            end
        end
    end)
end)
createSlider(miscContent, 186, "🔭 Camera FOV", 50, 120, 70, function(val)
    workspace.CurrentCamera.FieldOfView = val
end)
createSlider(miscContent, 248, "🌫 Fog Dichte", 0, 1000, 1000, function(val)
    game:GetService("Lighting").FogEnd = val * 100
end)
createToggle(miscContent, 310, "🔇 Sounds aus", function(state)
    for _, s in pairs(workspace:GetDescendants()) do
        if s:IsA("Sound") then s.Volume = state and 0 or 0.5 end
    end
end)
createToggle(miscContent, 354, "🎨 Rainbow Character", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if not char then return end
        local t = tick()
        for _, p in pairs(char:GetDescendants()) do
            if p:IsA("BasePart") then
                p.Color = Color3.fromHSV((t * 0.5) % 1, 1, 1)
            end
        end
    end)
end)
createToggle(miscContent, 398, "🔒 Lock Camera auf Spieler", function(state)
    RunService.RenderStepped:Connect(function()
        if not state then return end
        local nearest, minDist = nil, math.huge
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local tHrp = player.Character:FindFirstChild("HumanoidRootPart")
                if tHrp then
                    local d = (hrp.Position - tHrp.Position).Magnitude
                    if d < minDist then minDist = d; nearest = tHrp end
                end
            end
        end
        if nearest then
            workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, nearest.Position)
        end
    end)
end)
createToggle(miscContent, 442, "💬 Chat Spam (E)", function(state)
    UserInputService.InputBegan:Connect(function(input, gp)
        if gp or not state then return end
        if input.KeyCode == Enum.KeyCode.P then
            game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")
            -- Chat msg
        end
    end)
end)
createToggle(miscContent, 486, "🌀 Spin Charakter", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(5), 0)
        end
    end)
end)

-- ════════════════════════════════
-- PLAYER TAB
-- ════════════════════════════════
local playerContent = Instance.new("ScrollingFrame")
playerContent.Size = UDim2.new(1, 0, 1, 0)
playerContent.BackgroundTransparency = 1
playerContent.BorderSizePixel = 0
playerContent.ScrollBarThickness = 3
playerContent.ScrollBarImageColor3 = Color3.fromRGB(180,0,0)
playerContent.CanvasSize = UDim2.new(0,0,0,480)
playerContent.Visible = false
playerContent.Parent = ContentArea

createSlider(playerContent, 10, "❤️ Max Health", 50, 500, 100, function(val)
    local char = LocalPlayer.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.MaxHealth = val; hum.Health = val end
    end
    StatusLabel.Text = "● HP: "..val; StatusLabel.TextColor3 = Color3.fromRGB(255,100,100)
end)
createToggle(playerContent, 72, "🩹 Auto Heal", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health < hum.MaxHealth then
                hum.Health = math.min(hum.Health + 0.5, hum.MaxHealth)
            end
        end
    end)
end)
createToggle(playerContent, 116, "☠️ God Mode", function(state)
    local char = LocalPlayer.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then
            if state then
                hum:GetPropertyChangedSignal("Health"):Connect(function()
                    if state then hum.Health = hum.MaxHealth end
                end)
            end
        end
    end
    StatusLabel.Text = state and "● God Mode: AN" or "● CoreX bereit"
    StatusLabel.TextColor3 = state and Color3.fromRGB(255,215,0) or Color3.fromRGB(50,220,100)
end)
createToggle(playerContent, 160, "🎭 Fake Lag", function(state)
    RunService.Heartbeat:Connect(function()
        if not state then return end
        wait(0.1)
    end)
end)
createSlider(playerContent, 204, "💪 Jump Height", 50, 500, 50, function(val)
    local char = LocalPlayer.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.JumpHeight = val end
    end
end)
createToggle(playerContent, 266, "🚶 Anti Ragdoll", function(state)
    RunService.Stepped:Connect(function()
        if not state then return end
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false) end
        end
    end)
end)
createToggle(playerContent, 310, "🔁 Teleport zu letztem Tod", function(state)
    local lastDeathPos = nil
    LocalPlayer.CharacterAdded:Connect(function(char)
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")
        hum.Died:Connect(function()
            lastDeathPos = hrp.Position
        end)
    end)
    if state and lastDeathPos then
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.CFrame = CFrame.new(lastDeathPos) end
        end
    end
end)

-- TABS BAUEN
local tabNames = {"⚡ Speed", "👁 ESP", "👤 Player", "⚙️ Misc"}
local contentList = {speedContent, espContent, playerContent, miscContent}
local tabs = {}

for i, name in ipairs(tabNames) do
    local tab = Instance.new("TextButton")
    tab.Size = UDim2.new(0, 78, 1, 0)
    tab.BackgroundColor3 = Color3.fromRGB(28, 28, 40)
    tab.Text = name
    tab.TextColor3 = Color3.fromRGB(140, 140, 160)
    tab.TextSize = 11
    tab.Font = Enum.Font.GothamBold
    tab.BorderSizePixel = 0
    tab.Parent = TabFrame
    Instance.new("UICorner", tab).CornerRadius = UDim.new(0, 7)
    tabs[i] = tab
    tab.MouseButton1Click:Connect(function()
        for j, t in ipairs(tabs) do
            TweenService:Create(t, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(28,28,40)}):Play()
            t.TextColor3 = Color3.fromRGB(140,140,160)
            contentList[j].Visible = false
        end
        TweenService:Create(tab, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(160,0,0)}):Play()
        tab.TextColor3 = Color3.fromRGB(255,255,255)
        contentList[i].Visible = true
    end)
end

tabs[1].BackgroundColor3 = Color3.fromRGB(160, 0, 0)
tabs[1].TextColor3 = Color3.fromRGB(255, 255, 255)
speedContent.Visible = true

-- F4 TOGGLE
UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Enum.KeyCode.F4 then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

-- ANIMATION
MainFrame.Size = UDim2.new(0, 0, 0, 0)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back), {
    Size = UDim2.new(0, 370, 0, 520),
    Position = UDim2.new(0.5, -185, 0.5, -260)
}):Play()

print("[CoreX | Mörder Mystery] ✅ Geladen! F4 = Toggle")
