-- Orion laden (funktionierender Link)
local OrionLib = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Orion/main/source'))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

local Window = OrionLib:MakeWindow({
    Name = "🔪 CoreX | Mörder Mystery",
    HidePremium = true,
    SaveConfig = false,
    ConfigFolder = "CoreX",
    IntroEnabled = true,
    IntroText = "CoreX | Mörder Mystery",
    IntroIcon = "rbxassetid://4483362458",
    Icon = "rbxassetid://4483362458",
})

-- ════════════════════════════════
-- SPEED TAB
-- ════════════════════════════════
local SpeedTab = Window:MakeTab({
    Name = "⚡ Speed",
    Icon = "rbxassetid://4483362458",
    PremiumOnly = false,
})

SpeedTab:AddSection({
    Name = "Bewegung"
})

SpeedTab:AddSlider({
    Name = "Walk Speed",
    Min = 16,
    Max = 150,
    Default = 16,
    Color = Color3.fromRGB(255, 50, 50),
    Increment = 1,
    ValueName = "WS",
    Callback = function(val)
        local char = LocalPlayer.Character
        if char then
            local h = char:FindFirstChildOfClass("Humanoid")
            if h then h.WalkSpeed = val end
        end
    end,
})

SpeedTab:AddSlider({
    Name = "Jump Power",
    Min = 50,
    Max = 400,
    Default = 50,
    Color = Color3.fromRGB(255, 50, 50),
    Increment = 5,
    ValueName = "JP",
    Callback = function(val)
        local char = LocalPlayer.Character
        if char then
            local h = char:FindFirstChildOfClass("Humanoid")
            if h then h.JumpPower = val end
        end
    end,
})

SpeedTab:AddSlider({
    Name = "Gravity",
    Min = 10,
    Max = 196,
    Default = 196,
    Color = Color3.fromRGB(255, 50, 50),
    Increment = 1,
    ValueName = "G",
    Callback = function(val)
        workspace.Gravity = val
    end,
})

SpeedTab:AddSection({
    Name = "Modi"
})

SpeedTab:AddToggle({
    Name = "Infinite Jump",
    Default = false,
    Callback = function(state)
        UserInputService.JumpRequest:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if char then
                local h = char:FindFirstChildOfClass("Humanoid")
                if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end
            end
        end)
    end,
})

SpeedTab:AddToggle({
    Name = "No Clip",
    Default = false,
    Callback = function(state)
        RunService.Stepped:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if char then
                for _, p in pairs(char:GetDescendants()) do
                    if p:IsA("BasePart") then p.CanCollide = false end
                end
            end
        end)
    end,
})

SpeedTab:AddToggle({
    Name = "Fly Modus",
    Default = false,
    Callback = function(state)
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        if state then
            local bg = Instance.new("BodyGyro")
            bg.MaxTorque = Vector3.new(9e9,9e9,9e9)
            bg.P = 9e4
            bg.Parent = hrp
            local bv = Instance.new("BodyVelocity")
            bv.Velocity = Vector3.zero
            bv.MaxForce = Vector3.new(9e9,9e9,9e9)
            bv.Parent = hrp
            RunService.Heartbeat:Connect(function()
                if not state then
                    bg:Destroy(); bv:Destroy(); return
                end
                local cam = workspace.CurrentCamera
                local spd = 50
                local vel = Vector3.zero
                if UserInputService:IsKeyDown(Enum.KeyCode.W) then vel += cam.CFrame.LookVector*spd end
                if UserInputService:IsKeyDown(Enum.KeyCode.S) then vel -= cam.CFrame.LookVector*spd end
                if UserInputService:IsKeyDown(Enum.KeyCode.A) then vel -= cam.CFrame.RightVector*spd end
                if UserInputService:IsKeyDown(Enum.KeyCode.D) then vel += cam.CFrame.RightVector*spd end
                if UserInputService:IsKeyDown(Enum.KeyCode.Space) then vel += Vector3.new(0,spd,0) end
                if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then vel -= Vector3.new(0,spd,0) end
                bv.Velocity = vel
                bg.CFrame = cam.CFrame
            end)
        end
    end,
})

SpeedTab:AddToggle({
    Name = "Auto Respawn",
    Default = false,
    Callback = function(state)
        RunService.Heartbeat:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health <= 0 then
                    wait(1); LocalPlayer:LoadCharacter()
                end
            end
        end)
    end,
})

SpeedTab:AddSection({
    Name = "Aktionen"
})

SpeedTab:AddButton({
    Name = "Teleport zu Spawn",
    Callback = function()
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local spawn = workspace:FindFirstChild("SpawnLocation")
            if hrp and spawn then
                hrp.CFrame = spawn.CFrame + Vector3.new(0,5,0)
            end
        end
    end,
})

SpeedTab:AddButton({
    Name = "Charakter neu laden",
    Callback = function()
        LocalPlayer:LoadCharacter()
    end,
})

-- ════════════════════════════════
-- ESP TAB
-- ════════════════════════════════
local ESPTab = Window:MakeTab({
    Name = "👁 ESP",
    Icon = "rbxassetid://4483362458",
    PremiumOnly = false,
})

ESPTab:AddSection({
    Name = "🔴 Mörder = Rot  |  🔵 Sheriff = Blau  |  🟢 Normal = Grün"
})

local highlights = {}
local espActive = false

local function clearHighlights()
    for _, h in pairs(highlights) do
        if h and h.Parent then h:Destroy() end
    end
    highlights = {}
end

local function applyHighlights()
    clearHighlights()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hl = Instance.new("Highlight")
            hl.Adornee = player.Character
            local tool = player.Character:FindFirstChildOfClass("Tool")
            if tool then
                local n = tool.Name:lower()
                if n:find("knife") or n:find("blade") or n:find("murder") then
                    hl.FillColor = Color3.fromRGB(255, 30, 30)
                    hl.OutlineColor = Color3.fromRGB(200, 0, 0)
                elseif n:find("gun") or n:find("sheriff") then
                    hl.FillColor = Color3.fromRGB(30, 100, 255)
                    hl.OutlineColor = Color3.fromRGB(0, 60, 200)
                else
                    hl.FillColor = Color3.fromRGB(30, 220, 80)
                    hl.OutlineColor = Color3.fromRGB(0, 180, 50)
                end
            else
                hl.FillColor = Color3.fromRGB(30, 220, 80)
                hl.OutlineColor = Color3.fromRGB(0, 180, 50)
            end
            hl.FillTransparency = 0.4
            hl.OutlineTransparency = 0
            hl.Parent = player.Character
            table.insert(highlights, hl)
        end
    end
end

RunService.Heartbeat:Connect(function()
    if espActive then applyHighlights() end
end)

ESPTab:AddToggle({
    Name = "Player ESP",
    Default = false,
    Callback = function(state)
        espActive = state
        if not state then clearHighlights() end
    end,
})

ESPTab:AddToggle({
    Name = "Nametags",
    Default = false,
    Callback = function(state)
        RunService.RenderStepped:Connect(function()
            if not state then return end
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local head = player.Character:FindFirstChild("Head")
                    if head and not head:FindFirstChild("CoreXName") then
                        local bb = Instance.new("BillboardGui")
                        bb.Name = "CoreXName"
                        bb.Size = UDim2.new(0,120,0,30)
                        bb.StudsOffset = Vector3.new(0,3,0)
                        bb.AlwaysOnTop = true
                        bb.Parent = head
                        local lbl = Instance.new("TextLabel")
                        lbl.Size = UDim2.new(1,0,1,0)
                        lbl.BackgroundTransparency = 1
                        lbl.Text = player.Name
                        lbl.TextColor3 = Color3.fromRGB(255,255,255)
                        lbl.TextSize = 14
                        lbl.Font = Enum.Font.GothamBold
                        lbl.TextStrokeTransparency = 0
                        lbl.Parent = bb
                    end
                end
            end
        end)
    end,
})

ESPTab:AddToggle({
    Name = "Distanz ESP",
    Default = false,
    Callback = function(state)
        RunService.RenderStepped:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local tHrp = player.Character:FindFirstChild("HumanoidRootPart")
                    local head = player.Character:FindFirstChild("Head")
                    if tHrp and head then
                        local dist = math.floor((hrp.Position - tHrp.Position).Magnitude)
                        local bb = head:FindFirstChild("CoreXDist")
                        if not bb then
                            bb = Instance.new("BillboardGui")
                            bb.Name = "CoreXDist"
                            bb.Size = UDim2.new(0,80,0,20)
                            bb.StudsOffset = Vector3.new(0,5,0)
                            bb.AlwaysOnTop = true
                            bb.Parent = head
                            local dl = Instance.new("TextLabel")
                            dl.Name = "DistLabel"
                            dl.Size = UDim2.new(1,0,1,0)
                            dl.BackgroundTransparency = 1
                            dl.TextColor3 = Color3.fromRGB(255,220,50)
                            dl.TextSize = 12
                            dl.Font = Enum.Font.GothamBold
                            dl.TextStrokeTransparency = 0
                            dl.Parent = bb
                        end
                        local dl = bb:FindFirstChild("DistLabel")
                        if dl then dl.Text = dist.."m" end
                    end
                end
            end
        end)
    end,
})

ESPTab:AddToggle({
    Name = "Health ESP",
    Default = false,
    Callback = function(state)
        RunService.RenderStepped:Connect(function()
            if not state then return end
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local head = player.Character:FindFirstChild("Head")
                    local hum = player.Character:FindFirstChildOfClass("Humanoid")
                    if head and hum then
                        local bb = head:FindFirstChild("CoreXHP")
                        if not bb then
                            bb = Instance.new("BillboardGui")
                            bb.Name = "CoreXHP"
                            bb.Size = UDim2.new(0,80,0,18)
                            bb.StudsOffset = Vector3.new(0,1,0)
                            bb.AlwaysOnTop = true
                            bb.Parent = head
                            local hl2 = Instance.new("TextLabel")
                            hl2.Name = "HPLabel"
                            hl2.Size = UDim2.new(1,0,1,0)
                            hl2.BackgroundTransparency = 1
                            hl2.TextSize = 12
                            hl2.Font = Enum.Font.GothamBold
                            hl2.TextStrokeTransparency = 0
                            hl2.Parent = bb
                        end
                        local hl2 = bb:FindFirstChild("HPLabel")
                        if hl2 then
                            local hp = math.floor(hum.Health)
                            hl2.Text = "❤️ "..hp
                            hl2.TextColor3 = hp > 50 and Color3.fromRGB(50,255,50) or Color3.fromRGB(255,50,50)
                        end
                    end
                end
            end
        end)
    end,
})

ESPTab:AddToggle({
    Name = "Waffen ESP",
    Default = false,
    Callback = function(state)
        RunService.RenderStepped:Connect(function()
            if not state then return end
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local head = player.Character:FindFirstChild("Head")
                    local tool = player.Character:FindFirstChildOfClass("Tool")
                    if head then
                        local bb = head:FindFirstChild("CoreXWeapon")
                        if not bb then
                            bb = Instance.new("BillboardGui")
                            bb.Name = "CoreXWeapon"
                            bb.Size = UDim2.new(0,120,0,18)
                            bb.StudsOffset = Vector3.new(0,7,0)
                            bb.AlwaysOnTop = true
                            bb.Parent = head
                            local wl = Instance.new("TextLabel")
                            wl.Name = "WeaponLabel"
                            wl.Size = UDim2.new(1,0,1,0)
                            wl.BackgroundTransparency = 1
                            wl.TextSize = 11
                            wl.Font = Enum.Font.GothamBold
                            wl.TextStrokeTransparency = 0
                            wl.Parent = bb
                        end
                        local wl = bb:FindFirstChild("WeaponLabel")
                        if wl then
                            wl.Text = tool and "🔧 "..tool.Name or "❌ Keine"
                            wl.TextColor3 = tool and Color3.fromRGB(255,200,50) or Color3.fromRGB(180,180,180)
                        end
                    end
                end
            end
        end)
    end,
})

ESPTab:AddButton({
    Name = "Alle ESP entfernen",
    Callback = function()
        clearHighlights()
        for _, player in pairs(Players:GetPlayers()) do
            if player.Character then
                for _, obj in pairs(player.Character:GetDescendants()) do
                    if obj:IsA("Highlight") or obj:IsA("BillboardGui") then
                        obj:Destroy()
                    end
                end
            end
        end
    end,
})

-- ════════════════════════════════
-- PLAYER TAB
-- ════════════════════════════════
local PlayerTab = Window:MakeTab({
    Name = "👤 Player",
    Icon = "rbxassetid://4483362458",
    PremiumOnly = false,
})

PlayerTab:AddSection({ Name = "Stats" })

PlayerTab:AddSlider({
    Name = "Max Health",
    Min = 50, Max = 500, Default = 100,
    Color = Color3.fromRGB(255, 50, 50),
    Increment = 10,
    ValueName = "HP",
    Callback = function(val)
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum.MaxHealth = val; hum.Health = val end
        end
    end,
})

PlayerTab:AddSlider({
    Name = "FOV",
    Min = 50, Max = 120, Default = 70,
    Color = Color3.fromRGB(255, 50, 50),
    Increment = 1,
    ValueName = "°",
    Callback = function(val)
        workspace.CurrentCamera.FieldOfView = val
    end,
})

PlayerTab:AddSection({ Name = "Toggles" })

PlayerTab:AddToggle({
    Name = "Auto Heal",
    Default = false,
    Callback = function(state)
        RunService.Heartbeat:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum and hum.Health < hum.MaxHealth then
                    hum.Health = math.min(hum.Health + 0.5, hum.MaxHealth)
                end
            end
        end)
    end,
})

PlayerTab:AddToggle({
    Name = "God Mode",
    Default = false,
    Callback = function(state)
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:GetPropertyChangedSignal("Health"):Connect(function()
                    if state then hum.Health = hum.MaxHealth end
                end)
            end
        end
    end,
})

PlayerTab:AddToggle({
    Name = "Anti Ragdoll",
    Default = false,
    Callback = function(state)
        RunService.Stepped:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                end
            end
        end)
    end,
})

PlayerTab:AddToggle({
    Name = "Unsichtbar",
    Default = false,
    Callback = function(state)
        local char = LocalPlayer.Character
        if char then
            for _, p in pairs(char:GetDescendants()) do
                if p:IsA("BasePart") or p:IsA("Decal") then
                    p.Transparency = state and 1 or 0
                end
            end
        end
    end,
})

PlayerTab:AddToggle({
    Name = "Rainbow Character",
    Default = false,
    Callback = function(state)
        RunService.Heartbeat:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if not char then return end
            local t = tick()
            for _, p in pairs(char:GetDescendants()) do
                if p:IsA("BasePart") then
                    p.Color = Color3.fromHSV((t*0.5)%1, 1, 1)
                end
            end
        end)
    end,
})

PlayerTab:AddToggle({
    Name = "Spin Charakter",
    Default = false,
    Callback = function(state)
        RunService.Heartbeat:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.rad(5), 0) end
        end)
    end,
})

PlayerTab:AddToggle({
    Name = "Einfrieren",
    Default = false,
    Callback = function(state)
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.Anchored = state end
        end
    end,
})

PlayerTab:AddSection({ Name = "Aktionen" })

PlayerTab:AddButton({
    Name = "Reset Charakter",
    Callback = function()
        local char = LocalPlayer.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum.Health = 0 end
        end
    end,
})

-- ════════════════════════════════
-- MISC TAB
-- ════════════════════════════════
local MiscTab = Window:MakeTab({
    Name = "⚙️ Misc",
    Icon = "rbxassetid://4483362458",
    PremiumOnly = false,
})

MiscTab:AddSection({ Name = "Welt" })

MiscTab:AddToggle({
    Name = "Fullbright",
    Default = false,
    Callback = function(state)
        local L = game:GetService("Lighting")
        L.Brightness = state and 6 or 1
        L.FogEnd = state and 9e9 or 100000
        L.GlobalShadows = not state
    end,
})

MiscTab:AddSlider({
    Name = "Fog Dichte",
    Min = 0, Max = 1000, Default = 1000,
    Color = Color3.fromRGB(255, 50, 50),
    Increment = 10,
    ValueName = "",
    Callback = function(val)
        game:GetService("Lighting").FogEnd = val * 100
    end,
})

MiscTab:AddSection({ Name = "Gameplay" })

MiscTab:AddToggle({
    Name = "Anti AFK",
    Default = false,
    Callback = function(state)
        if state then
            local VU = game:GetService("VirtualUser")
            LocalPlayer.Idled:Connect(function()
                VU:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                wait(1)
                VU:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end)
        end
    end,
})

MiscTab:AddToggle({
    Name = "Auto Coins sammeln",
    Default = false,
    Callback = function(state)
        RunService.Heartbeat:Connect(function()
            if not state then return end
            local char = LocalPlayer.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            for _, obj in pairs(workspace:GetDescendants()) do
                if (obj.Name == "Coin" or obj.Name == "CoinDrop") and obj:IsA("BasePart") then
                    if (hrp.Position - obj.Position).Magnitude < 60 then
                        hrp.CFrame = CFrame.new(obj.Position)
                    end
                end
            end
        end)
    end,
})

MiscTab:AddToggle({
    Name = "Sounds aus",
    Default = false,
    Callback = function(state)
        for _, s in pairs(workspace:GetDescendants()) do
            if s:IsA("Sound") then s.Volume = state and 0 or 0.5 end
        end
    end,
})

MiscTab:AddToggle({
    Name = "Kamera Lock",
    Default = false,
    Callback = function(state)
        RunService.RenderStepped:Connect(function()
            if not state then return end
            local nearest, minDist = nil, math.huge
            local char = LocalPlayer.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local tHrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if tHrp then
                        local d = (hrp.Position - tHrp.Position).Magnitude
                        if d < minDist then minDist = d; nearest = tHrp end
                    end
                end
            end
            if nearest then
                workspace.CurrentCamera.CFrame = CFrame.new(
                    workspace.CurrentCamera.CFrame.Position, nearest.Position
                )
            end
        end)
    end,
})

MiscTab:AddSection({ Name = "Aktionen" })

MiscTab:AddButton({
    Name = "Teleport zu Spawn",
    Callback = function()
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local spawn = workspace:FindFirstChild("SpawnLocation")
            if hrp and spawn then
                hrp.CFrame = spawn.CFrame + Vector3.new(0,5,0)
            end
        end
    end,
})

MiscTab:AddButton({
    Name = "Charakter neu laden",
    Callback = function()
        LocalPlayer:LoadCharacter()
    end,
})

OrionLib:Init()
print("[CoreX | Mörder Mystery] ✅ Geladen!")
